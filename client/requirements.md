## Packages
framer-motion | Page transitions and animations
date-fns | Human-readable date formatting
lucide-react | High-quality icons
recharts | Data visualization for dashboards (if needed)

## Notes
- Tailwind config should have display font 'Outfit' and sans font 'DM Sans'.
- API responses use JSON strings for dates, ensure proper parsing.
- Missing endpoints will fail safely with Toast notifications.
- Using existing Shadcn UI components but heavily customized via Tailwind classes for a premium look.
